{
  "name": "arrow-parking-homepage",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "curl -sSfL -O https://raw.githubusercontent.com/aquaproj/aqua-installer/v3.1.1/aqua-installer && chmod +x aqua-installer && ./aqua-installer && rm aqua-installer && aqua i -l && pnpm install",
  "remoteEnv": {
    "PATH": "${containerEnv:HOME}/.local/share/aquaproj-aqua/bin:${containerEnv:PATH}"
  },
  "forwardPorts": [3000],
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "bradlc.vscode-tailwindcss"
      ]
    }
  }
}
